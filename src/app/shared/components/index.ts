export * from './table/table.component';
export * from './form/form.component';
export * from './button/button.component';

/*
import {
  CellConfig,
  Paginator,
  TableHeaderConfig,
  ITableConfig,
  IBody,
} from '../../common/models/Table/Table-config';

const headerConfig: Array<TableHeaderConfig> = [
  new TableHeaderConfig({
    value: 'name',
    sort: true,
  }),
  new TableHeaderConfig({
    value: 'lastName',
  }),
  new TableHeaderConfig({
    value: 'average mark',
  }),
  new TableHeaderConfig({
    value: '1583915651415',
    sort: true,
    datePicker: true,
  }),
];

const bodyConfig: Array<IBody> = [
  {
    'name': new CellConfig({
      value: 'Unknown1',
    }),
    'lastName': new CellConfig({
      value: 'Unknown1',
    }),
    'average mark': new CellConfig({}),
  },
  {
    'name': new CellConfig({
      value: 'Unknown2',
    }),
    'lastName': new CellConfig({
      value: 'Unknown2',
    }),
    'average mark': new CellConfig({}),
  },
  {
    'name': new CellConfig({
      value: 'Unknown3',
    }),
    'lastName': new CellConfig({
      value: 'Unknown3',
    }),
    'average mark': new CellConfig({}),
  },
];

const paginatorConfig: Paginator = new Paginator({
  defaultPageSize: 5,
});

export const tableConfig: ITableConfig = {
  headers: headerConfig,
  body: bodyConfig,
  paginator: paginatorConfig,
};

*/
